# 系統後端資料需求與待辦事項

這份文件記錄了目前前端為了呈現完整 UI/UX，暫時採用「前端模擬資料（Mock Data）」的部分。
請後端團隊後續協助實作以下排程與 API，以便將系統全面切換為真實數據。

## 需求一：全市場每日漲跌幅同步 (供列表頁與篩選使用)

*   **目前狀況**：前端 `StockSearchPage` 的「領漲/領跌 (Gainers/Losers)」篩選功能，目前依賴前端產生的固定假資料。因為如果在列表頁直接透過即時爬蟲打 2300 檔股票的 API，會導致伺服器 IP 被交易所封鎖。
*   **後端實作目標**：
    1.  **建立背景排程 (Cron Job)**：例如使用 `apscheduler`，在每天台股盤後（約下午 2:00 後），批次抓取全市場所有股票的收盤價、漲跌價、漲跌幅與成交量，並同步更新到資料庫中（可以更新原有的 `stocks` 表，或是建立獨立的 `daily_summaries` 表）。
    2.  **更新 API 回傳格式**：修改 `GET /api/v1/stocks` 的回傳 Schema (`StockRead`)，加入 `change` (漲跌價) 與 `change_percent` (漲跌幅) 等欄位。
*   **後續動作**：API 完成後，通知前端工程師移除 `StockSearchPage.tsx` 中的 `getMockChange` 函數，改接真實的 `change_percent`。

## 需求二：產業熱力圖資料聚合 (Industry Heatmap)

*   **目前狀況**：首頁 (`DashboardPage`) 下方的「市場熱力圖 (Market Heatmap)」，目前使用的是前端寫死的 `MOCK_INDUSTRIES` 陣列。
*   **後端實作目標**：
    1.  **新增 API Endpoint**：建立一支新的 API（建議路徑：`GET /api/v1/market/heatmap`）。
    2.  **資料聚合邏輯**：在後端將當日全市場的股票，依據 `industry` (產業類別) 進行分組。
    3.  **計算與回傳格式**：計算出每個產業的「權重」(例如該產業總市值或總成交量佔比) 與「平均漲跌幅」。回傳 JSON 格式需包含以下結構：
        ```json
        [
          { "name": "半導體業", "weight": 6, "change": 2.4 },
          { "name": "金融保險業", "weight": 4, "change": 0.8 },
          { "name": "航運業", "weight": 3, "change": -1.5 }
        ]
        ```
*   **後續動作**：API 開發並測試完成後，通知前端移除首頁的 `MOCK_INDUSTRIES`，改用 API 獲取真實產業熱度資料。